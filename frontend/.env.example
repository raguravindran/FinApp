# Optional for local/Vite usage against a separate backend
# If unset, frontend calls /api/emi/ on same origin (Netlify redirect -> function).
# Public URL of the backend API root (no trailing slash)
# Example for deployed backend: https://your-backend.example.com
VITE_API_BASE_URL=http://127.0.0.1:8000
