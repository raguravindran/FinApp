export function runSimpleProjection(prompt: string): string {
  return [
    'Deterministic calculator route selected.',
    'A detailed projection engine is not wired yet.',
    `Input received: "${prompt}"`,
    'Next step: connect to dedicated finance calculators for SIP, debt payoff, and goal maturity.',
  ].join(' ');
}
